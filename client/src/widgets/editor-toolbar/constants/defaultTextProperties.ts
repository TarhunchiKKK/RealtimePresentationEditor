import { FontFamilies, FontSizes, TextColors, TextHeadings } from "../../../shared/editor";

export const defaultTextHeading = TextHeadings.Main;
export const defaultFontFamily = FontFamilies.SansSerif;
export const defaultFontSize = FontSizes.Text16;
export const defaultTextColor = TextColors.Black;
